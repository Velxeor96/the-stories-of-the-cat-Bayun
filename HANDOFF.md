# 📋 HANDOFF: WARHAMMER 40K RPG PLATFORM

**Дата:** 2026-09-18 (обновлено)
**Версия:** 1.1
**Статус:** Спринт 0 — 5/8 задач закрыто, ядро работает

---

## 🎯 ВИДЕНИЕ ПРОЕКТА

**Что строим:** Платформа для текстовых RPG с мульти-ИИ Мастером.
**Пилот:** Warhammer 40,000 Rogue Trader.
**Цель:** Настоящая игра с честными бросками, синхронизированным состоянием,
каноничным лором и ведущим Мастером — **не чат с нейросетью**.

**Долгосрочно:** Мультисистемная платформа (D&D, Cyberpunk, Witcher, Cthulhu,
конструктор) с маркетплейсом кампаний и мультиплеером.

---

## 🏗️ АРХИТЕКТУРА (мульти-ИИ)

**Главный принцип:** Состояние (XP, HP, порча, инвентарь, репутация, квесты, NPC) —
ВСЁ В PYTHON. ИИ только читает и озвучивает. Никаких правил в промте.

| Роль | Модель | Задача |
|---|---|---|
| Python-ядро | — | Состояние, броски, триггеры, RAG, оркестрация, сессии |
| Мастер-рассказчик | DeepSeek V4 Pro | Художественный текст, сцены, NPC |
| Аналитик команд | DeepSeek Flash / Groq | Парсинг намерений игрока в JSON |
| Модератор | llama-guard (локально) | Мягкий отказ, не цензурная заглушка |
| Арбитр | DeepSeek V4 Pro | Спорные случаи модерации (1-2% ходов) |
| Лор-консультант | DeepSeek Flash + RAG | Канонические ответы |
| Художник | YandexART | Картинки локаций, NPC |
| Резерв | GigaChat | Российский, без VPN |

**Схема хода:**
Ввод игрока → [Аналитик] → JSON → [Python: броски/триггеры]
→ [Мастер] → текст → [Модератор] → [Python: применение эффектов]
→ Вывод игроку

text

---

## 🖥️ СТЕК

- Python 3.13
- Streamlit — веб-интерфейс
- ChromaDB — векторная база (4624 чанка)
- sentence-transformers (multilingual-e5-small)
- openai SDK — для DeepSeek (OpenAI-совместим)
- torch 2.6+cu124 — GPU (GTX 1660)

**Путь проекта:** `C:\Users\79109\Desktop\my_game\`
**Репозиторий:** https://github.com/Velxeor96/the-stories-of-the-cat-Bayun
**Онлайн:** https://the-stories-of-the-cat-bayun-rpg.streamlit.app

---

## 📁 СТРУКТУРА ПРОЕКТА (актуальная)
my_game/
├── core/ ← НОВОЕ ЯДРО (Python-логика)
│ ├── init.py
│ ├── state.py ✅ менеджер состояния
│ ├── roll_engine.py ✅ движок бросков (roll-under d100)
│ ├── trigger_engine.py ✅ движок триггеров (event → effects)
│ ├── death.py ✅ Death State Machine
│ ├── errors.py ✅ иерархия ошибок LLM
│ └── llm_client.py ✅ клиент LLM с ретраями
├── rules/
│ └── wh40k_triggers.json ✅ таблица триггеров (данные)
├── scripts/ ← пайплайн данных (не игровой)
│ ├── scraper.py
│ ├── clean_data.py
│ ├── build_embeddings.py
│ ├── init_db.py
│ ├── migrate_add_ids.py ✅ миграция UUID
│ ├── migrate_add_status.py ✅ миграция status
│ └── fix_chat_ids.py ✅ синхронизация ID
├── tests/ ← тесты + отчёты альфы
│ ├── test_roll_engine.py ✅ 28 проверок
│ ├── test_trigger_engine.py ✅ 19 проверок
│ ├── test_death.py ✅ 30 проверок
│ ├── test_llm_client.py ✅ ~30 проверок
│ └── test_function_calling.py
├── legacy/ ← СТАРЫЙ КОД (референс, не трогаем)
│ ├── app.py (161 КБ монолит)
│ ├── dice.py
│ ├── Game.py
│ └── test_master.py
├── docs/
│ └── screenshots/ (25 PNG из альфы)
├── characters/ ← сейвы
│ ├── Костопевец.json (с UUID + status)
│ └── Костопевец.chat.json (ID синхронизированы)
├── data/ ← лор (chaos, eldar, imperium, ...)
├── chroma_db/ ← векторная база (28 МБ)
├── prompts/
│ ├── master.txt (37 КБ, требует нарезки)
│ └── adaptation.txt
├── .streamlit/
│ ├── config.toml
│ └── secrets.toml ← API-ключи (в .gitignore)
├── character_creation.py
├── factions_data.py
├── knowledge.py
├── fallbacks_*.py
├── requirements.txt
├── HANDOFF.md ← ЭТОТ ФАЙЛ
└── .gitignore

text

---

## 🎯 ТЕКУЩИЙ СПРИНТ: 0 — ФУНДАМЕНТ

| # | Задача | Статус |
|---|---|---|
| 0.1 | session_id / player_id / character_id (UUID) | ✅ |
| 0.2 | `core/state.py` — менеджер состояния | ✅ |
| 0.3 | `core/roll_engine.py` — движок бросков | ✅ |
| 0.4 | `core/trigger_engine.py` — триггеры | ✅ |
| 0.5 | Death State Machine (`core/death.py`) | ✅ |
| 0.6 | Prompt Split (нарезка master.txt) | ⬜ |
| 0.7 | Error Handler (`core/errors.py`, `core/llm_client.py`) | ✅ |
| 0.8 | HANDOFF.md в корне | ✅ |

**Итог:** ядро состояния, бросков, триггеров, смерти и ретраев — работает и покрыто тестами.

---

## 🎯 СПРИНТ 1 (следующий)

- `core/analyst.py` — парсинг команд игрока в JSON (DeepSeek Flash)
- `core/master.py` — мастер-рассказчик (DeepSeek V4 Pro)
- `core/moderator.py` — модератор (regex + llama-guard)
- `core/orchestrator.py` — связка всего
- `core/session.py` — управление сессиями
- `config.yaml` — конфиг моделей
- Новый тонкий `app.py`

---

## 🚨 РЕЗУЛЬТАТЫ АЛЬФА-ТЕСТА (что было сломано)

Все P0-баги **адресованы** новым ядром:

1. ✅ Состояние синхронизировано → `core/state.py`
2. ✅ Броски верные (margin 18, не 0) → `core/roll_engine.py`
3. ⬜ Нарратив vs броски → задача `orchestrator`
4. ✅ Смерть блокирует игру → `core/death.py`
5. ⬜ Цензурные заглушки → задача `moderator`
6. ⬜ Мета-утечки → задача Prompt Split
7. ✅ Сессии различимы → UUID в `state.py`
8. ✅ 429 не сыпется игроку → `core/llm_client.py`
9. ⬜ RAG отключён → задача `orchestrator`

---

## 🔧 ФОРМАТ РАБОТЫ

**Пользователь:** новичок в IT, всё делает по инструкциям.
**Ассистент:** пошаговые инструкции, полные файлы, никаких «найди строку X».

**Правила:**
1. Новый файл → `type nul > путь` + `notepad путь` + полный код.
2. Правка → `notepad путь` + полностью обновлённый код.
3. После каждой правки: синтаксическая проверка → тест → push.
4. Не переходить к следующему шагу, пока текущий не подтверждён.
5. Push — только по команде «пушим».
6. В конце каждой реплики — счётчик чата.

---

## 📊 СИСТЕМА HANDOFF

**Счётчик чата в конце реплик ассистента:**
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 СТАТУС ЧАТА
Сообщений: ~N / ~200 (X%)
Файлов: N / 15 (Y%)
Статус: 🟢 / 🟡 / 🟠 / 🔴
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

text

**Пороги:**
- 🟢 0-50% — работаем
- 🟡 50-70% — «скоро новый чат»
- 🟠 70-85% — «пора делать HANDOFF»
- 🔴 85-100% — «закрываем немедленно»

**Протокол перехода:**
1. Пользователь: «Сделай HANDOFF».
2. Ассистент: обновляет этот файл + выдаёт блок для копирования.
3. Новый чат → вставить блок первым сообщением.

---

## 🎬 СЛЕДУЮЩИЙ ШАГ

**Задача 0.6:** нарезать `prompts/master.txt` (37 КБ) на:
- `prompts/master_core.txt` — стиль, тон, правила нарратива
- `prompts/master_lore.txt` → переносится в RAG
- `prompts/master_rules.txt` → УДАЛИТЬ (правила уходят в Python)

**После:** Спринт 1 — `config.yaml` + `analyst.py` + `master.py` + `orchestrator.py`.

---

## 🚫 ЧЕГО НЕ ДЕЛАТЬ

- ❌ Не фиксить всё промтом — переполнит контекст.
- ❌ Не добавлять цензурные заглушки — только игровые отказы.
- ❌ Не давать ИИ менять состояние напрямую.
- ❌ Не смешивать роли (Мастер ≠ модератор ≠ бухгалтер).
- ❌ Не игнорировать счётчик чата.

---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 **СТАТУС ЧАТА**
Сообщений: ~36 / ~200 (18%)
Файлов: 15 / 15 (100%) — все запланированные
Статус: 🟢 **продолжаем** — Спринт 0 закрыт на 6/8
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━